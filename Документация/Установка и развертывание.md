
# Установка и развертывание
## Локальный мессенджер для образовательных учреждений

**Версия документа:** 1.0  
**Дата:** 2026  
**Автор:** Малиневский Егор Сергеевич/21ИС-24

---

## Оглавление

1. [Обзор системы](#обзор-системы)
2. [Требования к окружению](#требования-к-окружению)
3. [Получение исходного кода](#получение-исходного-кода)
4. [Настройка проекта](#настройка-проекта)
5. [Установка зависимостей](#установка-зависимостей)
6. [Настройка сервера](#настройка-сервера)
7. [Настройка клиента](#настройка-клиента)
8. [Запуск системы](#запуск-системы)
9. [Проверка работоспособности](#проверка-работоспособности)
10. [Устранение неполадок](#устранение-неполадок)
11. [Полезные команды](#полезные-команды)
12. [Приложения](#приложения)

---

<a name="обзор-системы"></a>
## 1. Обзор системы

**Локальный мессенджер** — это клиент-серверное приложение для обмена сообщениями в пределах локальной сети образовательного учреждения. Система разработана как учебный проект для демонстрации навыков разработки клиент-серверных приложений.

### Архитектура:
```
┌─────────────────┐        ┌─────────────────┐
│   Клиентское    │        │   Серверное     │
│   приложение    │◄──────►│   приложение    │
│   (PyQt5)       │ HTTP   │   (FastAPI)     │
│                 │ WebSocket│                │
└─────────────────┘        └─────────────────┘
                                    │
                                    ▼
                            ┌─────────────────┐
                            │   База данных   │
                            │   (SQLite)      │
                            └─────────────────┘
```

### Ключевые особенности:
- ✅ **Real-time общение** через WebSocket
- ✅ **Безопасная аутентификация** с JWT токенами
- ✅ **Отправка изображений** с предварительным просмотром
- ✅ **Локальное хранение данных** без доступа в интернет
- ✅ **Кроссплатформенность** (Windows, Linux)

---

<a name="требования-к-окружению"></a>
## 2. Требования к окружению

### 2.1. Минимальные требования

| Компонент | Клиент | Сервер |
|-----------|---------|---------|
| **Операционная система** | Windows 10/11, Linux Ubuntu 20.04+ | Windows 10/11, Linux Ubuntu 20.04+ |
| **Процессор** | 1 ядро, 1 ГГц | 1 ядро, 1 ГГц |
| **Оперативная память** | 256 МБ | 512 МБ |
| **Свободное место на диске** | 50 МБ | 100 МБ |
| **Python версия** | 3.8 или выше | 3.8 или выше |
| **Сетевое подключение** | Локальная сеть (Ethernet/Wi-Fi) | Статический IP в локальной сети |

### 2.2. Проверка текущей системы

**Проверка Python:**
```bash
python --version
# Должно показать: Python 3.8.x или выше
# Если не работает, попробуйте:
python3 --version
```

**Проверка доступного места на диске:**
```bash
# Windows:
dir

# Linux/Mac:
df -h .
```

### 2.3. Установка недостающего ПО

**Если Python не установлен:**

**Windows:**
1. Скачайте Python с [python.org](https://www.python.org/downloads/)
2. Установите, **обязательно отметьте галочку "Add Python to PATH"**
3. Перезагрузите компьютер

**Linux (Ubuntu/Debian):**
```bash
sudo apt update
sudo apt install python3 python3-pip python3-venv
```

**MacOS:**
```bash
brew install python
```

---

<a name="получение-исходного-кода"></a>
## 3. Получение исходного кода

### 3.1. Способ 1: Клонирование репозитория Git (рекомендуется)

#### Установка Git:
**Windows:** Скачайте с [git-scm.com](https://git-scm.com/download/win)  
**Linux:** `sudo apt install git`  
**Mac:** `brew install git`

#### Клонирование проекта:
```bash
# Откройте терминал или командную строку
# Перейдите в папку, где хотите сохранить проект
cd C:\Users\ВашеИмя\Projects  # Для Windows
# Или
cd ~/projects  # Для Linux/Mac

# Клонируйте репозиторий
git clone https://github.com/Leendeseqy/PKOvchinnikova_21IS_4semestr_Malinevskiy.git
cd PKOvchinnikova_21IS_4semestr_Malinevskiy

# Перейдите в папку с мессенджером
cd messenger
```

### 3.2. Способ 2: Скачивание ZIP архива

1. Перейдите на страницу проекта: [https://github.com/Leendeseqy/PKOvchinnikova_21IS_4semestr_Malinevskiy](https://github.com/Leendeseqy/PKOvchinnikova_21IS_4semestr_Malinevskiy)
2. Нажмите кнопку **"Code"** → **"Download ZIP"**
3. Распакуйте архив в нужную папку
4. Перейдите в распакованную папку:
```bash
cd PKOvchinnikova_21IS_4semestr_Malinevskiy
cd messenger
```

### 3.3. Проверка структуры проекта

После получения кода вы должны увидеть следующую структуру:

```
📦 Репозиторий с мессенджером 
├── 📄 requirements.txt
├── 📁 client/                       # Исходный код клиента
│   ├── 📄 main.py                        # Точка входа клиента
│   ├── 📄 config.py                      # Конфигурация подключения
│   ├── 📄 websocket_client.py            # WebSocket клиент
│   ├── 📁 models/
│   │   ├── 📄 message.py                     # Модель сообщения
│   │   └── 📄 user.py                        # Модель пользователя
│   └── 📁 ui/
│       ├── 📄 login_dialog.py                # Окно входа/регистрации
│       ├── 📄 main_window.py                 # Главное окно приложения
│       └── 📄 chat_widget.py                 # Виджет чата
│   
├── 📁 server/                       # Исходный код сервера
│   ├── 📄 main.py                        # Точка входа сервера (FastAPI)
│   ├── 📄 websocket_manager.py           # Менеджер WebSocket соединений
│   ├── 📄 dependencies.py                # Зависимости (JWT аутентификация)
│   ├── 📁 database/
│   │   ├── 📄 db.py                      # Инициализация БД
│   │   ├── 📄 user_model.py              # Модель пользователя
│   │   ├── 📄 models.py
│   │   └── 📄 message_model.py           # Модель сообщения
│   ├── 📁 routers/
│   │   ├── 📄 auth.py                    # Аутентификация
│   │   ├── 📄 messages.py                # Работа с сообщениями
│   │   ├── 📄 users.py                   # Управление пользователями
│   │   └── 📄 admin.py                   # Административные функции
│   └── 📁 schemas/
│       ├── 📄 user.py                    # Pydantic схемы пользователей
│       └── 📄 message.py                 # Pydantic схемы сообщений
└── 📁 shared/
    └──📄 models.py
```


**Важно:** Все последующие команды выполняются из папки `messenger/`.

---

<a name="настройка-проекта"></a>
## 4. Настройка проекта

### 4.1. Создание виртуального окружения (рекомендуется)

Виртуальное окружение изолирует зависимости проекта и предотвращает конфликты с другими проектами.

```bash
# Находясь в папке messenger/
python -m venv venv
```

**Для Windows:**
```bash
# Активация виртуального окружения в Command Prompt:
venv\Scripts\activate

# В PowerShell (может потребоваться разрешить выполнение скриптов):
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
.\venv\Scripts\Activate.ps1
```

**Для Linux/Mac:**
```bash
source venv/bin/activate
```

**Проверка активации:**
Вы должны увидеть `(venv)` в начале командной строки:
```
(venv) C:\Users\ВашеИмя\Projects\PKOvchinnikova_21IS_4semestr_Malinevskiy\messenger>
```

### 4.2. Отключение виртуального окружения

Когда закончите работу с проектом:
```bash
deactivate
```

---

<a name="установка-зависимостей"></a>
## 5. Установка зависимостей

### 5.1. Установка всех зависимостей (рекомендуется)

**Важно:** Убедитесь, что виртуальное окружение активировано!

```bash
# Находясь в папке messenger/
pip install -r requirements.txt
```

### 5.2. Проверка установки

```bash
# Проверить установленные пакеты
pip list

# Или конкретные ключевые пакеты
pip show fastapi pyqt5 requests websockets
```

**Ожидаемый результат:**
```
Package    Version
---------  -------
fastapi    0.104.1
PyQt5      5.15.9
requests   2.31.0
websockets 12.0
uvicorn    0.24.0
# ... и другие зависимости
```

### 5.3. Решение проблем с установкой

**Проблема:** "pip is not recognized"
**Решение:**
```bash
# Используйте явный вызов через python
python -m pip install -r requirements.txt
```

**Проблема:** Ошибки при установке PyQt5
**Решение для Windows:**
```bash
pip install pyqt5 --user
```

**Решение для Linux (Ubuntu/Debian):**
```bash
sudo apt-get update
sudo apt-get install python3-pyqt5
sudo apt-get install qt5-default
```

**Решение для Mac:**
```bash
brew install pyqt5
pip install pyqt5
```

### 5.4. Обновление pip

Если возникают проблемы с установкой:
```bash
python -m pip install --upgrade pip
```

---

<a name="настройка-сервера"></a>
## 6. Настройка сервера

### 6.1. Определение IP-адреса сервера

**Важно:** Сервер должен иметь статический IP-адрес в локальной сети.

**Как узнать IP-адрес Windows:**
```cmd
ipconfig
# Найдите строку: IPv4 Address . . . . . . . . . . . : 192.168.x.x
```

**Как узнать IP-адрес Linux/Mac:**
```bash
ifconfig
# Или
ip addr show
```

### 6.2. Конфигурация сервера по умолчанию

Сервер настраивается автоматически:
- **Хост:** 0.0.0.0 (принимает подключения со всех интерфейсов)
- **Порт:** 8000
- **База данных:** `messenger.db` (создается автоматически в папке server/)

### 6.3. Изменение настроек сервера (опционально)

Если нужно изменить порт или другие параметры, отредактируйте файл `server/main.py`:

```python
# Найдите в конце файла:
if __name__ == "__main__":
    import uvicorn
    uvicorn.run(app, host="0.0.0.0", port=8000)
    
# Измените на нужные параметры:
uvicorn.run(app, host="192.168.0.100", port=8080)  # Пример
```

### 6.4. Ручная инициализация базы данных (опционально)

База данных создается автоматически при первом запуске сервера, но можно создать вручную:

```bash
# Находясь в папке messenger/
cd server

# Запустите инициализацию
python -c "from database.db import init_db; init_db()"
```

**Созданные таблицы:**
1. `users` - информация о пользователях
2. `messages` - история сообщений

---

<a name="настройка-клиента"></a>
## 7. Настройка клиента

### 7.1. Настройка подключения к серверу

**Критически важный шаг:** Перед запуском клиента необходимо настроить файл конфигурации.

Откройте файл `client/config.py` в текстовом редакторе:

```python
# client/config.py

# ЗАМЕНИТЕ ЭТОТ IP-АДРЕС НА РЕАЛЬНЫЙ IP СЕРВЕРА
SERVER_HOST = "192.168.0.100"  # ← ИЗМЕНИТЬ НА IP СЕРВЕРА
SERVER_PORT = 8000
SERVER_URL = f"http://{SERVER_HOST}:{SERVER_PORT}"
```

### 7.2. Проверка подключения

Перед запуском клиента убедитесь, что он может подключиться к серверу:

```bash
# На клиентской машине

# Проверка доступности сервера
ping 192.168.0.100  # Замените на IP сервера

# Проверка открытого порта
# Windows:
telnet 192.168.0.100 8000

# Linux/Mac:
nc -zv 192.168.0.100 8000
# Или
telnet 192.168.0.100 8000
```

### 7.3. Настройка нескольких клиентов

Для настройки нескольких клиентских машин:

1. **Повторите шаги 1-5** на каждой машине (клонирование, установка зависимостей)
2. **На каждой машине** отредактируйте `client/config.py` с IP сервера
3. **IP сервера должен быть одинаковым** во всех конфигурациях

---

<a name="запуск-системы"></a>
## 8. Запуск системы

### 8.1. Важная последовательность запуска

**Всегда запускайте в таком порядке:**

1. **Сначала сервер** (должен работать постоянно)
2. **Потом клиенты** (можно запускать/останавливать)

### 8.2. Запуск сервера

**Способ 1: Через Python (простой)**
```bash
# Находясь в папке messenger/
cd server
python main.py
```

**Способ 2: Через Uvicorn (рекомендуется для разработки)**
```bash
cd server
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

**Ожидаемый вывод сервера:**
```
INFO:     Started server process [1234]
INFO:     Waiting for application startup.
INFO:     Application startup complete.
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
```

### 8.3. Запуск клиента

```bash
# Находясь в папке messenger/
cd client
python main.py
```

**Ожидаемый результат:**
1. Появится окно входа в систему
2. Создайте нового пользователя или войдите существующим
3. После входа откроется главное окно мессенджера

### 8.4. Запуск нескольких клиентов на одном компьютере

Для тестирования можно запустить несколько клиентов на одном компьютере:

1. Запустите первый клиент как описано выше
2. Откройте новое окно терминала/командной строки
3. Повторите шаги для запуска второго клиента

### 8.5. Запуск в фоновом режиме (Linux/Mac)

**Сервер:**
```bash
cd server
nohup python main.py > server.log 2>&1 &
```

**Клиент:**
```bash
cd client
nohup python main.py > client.log 2>&1 &
```

**Проверка работающих процессов:**
```bash
ps aux | grep python
```

---

<a name="проверка-работоспособности"></a>
## 9. Проверка работоспособности

### 9.1. Проверка сервера

**Способ 1: Через браузер (рекомендуется)**
1. Откройте браузер на **любой** машине в сети
2. Перейдите по адресу: `http://192.168.0.100:8000/docs`
   (замените IP на адрес вашего сервера)
3. Должна открыться **автоматическая документация FastAPI (Swagger UI)**

**Способ 2: Через curl (терминал)**
```bash
# Проверить доступность API
curl http://192.168.0.100:8000/docs

# Проверить корневой endpoint
curl http://192.168.0.100:8000/
```

### 9.2. Тестовый сценарий работы

| Шаг | Действие | Ожидаемый результат |
|-----|----------|---------------------|
| 1 | Запустить сервер | `Uvicorn running on http://0.0.0.0:8000` |
| 2 | Запустить Клиент 1 | Появляется окно входа |
| 3 | Нажать "Регистрация" | Открывается форма регистрации |
| 4 | Зарегистрировать User1 | Сообщение "Registration successful" |
| 5 | Войти как User1 | Открывается главное окно |
| 6 | Запустить Клиент 2 | Появляется окно входа |
| 7 | Зарегистрировать User2 | Регистрация успешна |
| 8 | Войти как User2 | Открывается главное окно |
| 9 | User1: кликнуть на User2 | Открывается чат с User2 |
| 10 | User1: отправить "Привет!" | Сообщение появляется в чатах обоих пользователей |
| 11 | User2: ответить "Здравствуйте!" | Сообщение появляется в обоих чатах |

### 9.3. Проверка дополнительных функций

**Проверка отправки изображений:**
1. В открытом чате нажмите кнопку **📎**
2. Выберите файл изображения (.png, .jpg, .gif, .bmp)
3. Изображение должно появиться в чате

**Проверка удаления сообщений:**
1. Отправьте тестовое сообщение
2. Запомните ID сообщения (отображается под сообщением)
3. Правый клик на сообщении → "Delete Message"
4. Введите ID сообщения
5. Сообщение должно исчезнуть у обоих пользователей

**Проверка онлайн статусов:**
1. Закройте Клиент 2
2. В Клиенте 1 статус User2 должен измениться с 🟢 на ⚫
3. Запустите Клиент 2 снова
4. Статус должен вернуться к 🟢 (через 10 секунд)

---

<a name="устранение-неполадок"></a>
## 10. Устранение неполадок

### 10.1. Частые проблемы и решения

#### **Проблема 1: Сервер не запускается**
```
Ошибка: OSError: [Errno 98] Address already in use
```

**Решение:**
```bash
# Найдите процесс, использующий порт 8000
# Windows:
netstat -ano | findstr :8000
taskkill /PID <номер_PID> /F

# Linux/Mac:
lsof -i :8000
kill -9 <номер_PID>

# Или измените порт в server/main.py
```

#### **Проблема 2: Клиент не подключается к серверу**
```
Ошибка: Cannot connect to server
```

**Решение:**
1. Проверьте IP в `client/config.py`
2. Убедитесь, что сервер запущен
3. Проверьте сетевую доступность: `ping IP_сервера`
4. Проверьте порт: `telnet IP_сервера 8000`
5. Проверьте брандмауэр (разрешите порт 8000)

#### **Проблема 3: Не видны другие пользователи**

**Решение:**
1. Подождите 10 секунд (статусы обновляются по таймеру)
2. Убедитесь, что другие пользователи вошли в систему
3. Перезапустите клиент
4. Проверьте подключение к серверу

#### **Проблема 4: Не отправляются сообщения**

**Решение:**
1. Проверьте, что в поле ввода есть текст
2. Убедитесь, что выбран собеседник
3. Проверьте подключение к серверу
4. Попробуйте перезапустить клиент

#### **Проблема 5: Не отправляются изображения**

**Решение:**
1. Проверьте формат файла (PNG, JPG, JPEG, GIF, BMP)
2. Уменьшите размер изображения (рекомендуется до 5 МБ)
3. Попробуйте другое изображение
4. Проверьте права доступа к файлу

### 10.2. Сбор информации для диагностики

При обращении за помощью подготовьте:

```bash
# Информация о системе:
python --version
pip list

# Информация о сети:
ipconfig  # Windows
ifconfig  # Linux/Mac

# Логи сервера:
# Запустите сервер с выводом логов в файл
cd server
python main.py > server_log.txt 2>&1

# Логи клиента:
# Ошибки обычно показываются в окнах сообщений
```

---

<a name="полезные-команды"></a>
## 11. Полезные команды

### 11.1. Сетевые команды

```bash
# Проверить доступность сервера
ping 192.168.0.100

# Проверить открытые порты
# Windows:
netstat -an | findstr 8000

# Linux/Mac:
netstat -tuln | grep 8000

# Проверить связь с портом
# Windows PowerShell:
Test-NetConnection 192.168.0.100 -Port 8000

# Linux/Mac:
nc -zv 192.168.0.100 8000
```

### 11.2. Команды Python и проекта

```bash
# Активация виртуального окружения
# Windows:
venv\Scripts\activate

# Linux/Mac:
source venv/bin/activate

# Обновление всех зависимостей
pip install --upgrade -r requirements.txt

# Проверка структуры базы данных
cd server
sqlite3 messenger.db ".tables"
sqlite3 messenger.db ".schema messages"
```

### 11.3. Команды для работы с сервером

```bash
# Запуск сервера с логированием
cd server
python main.py > server.log 2>&1 &

# Просмотр логов сервера
tail -f server.log  # Linux/Mac
# Или откройте файл server.log в редакторе

# Остановка сервера
# Найдите PID процесса:
ps aux | grep python  # Linux/Mac
tasklist | findstr python  # Windows
# И остановите его
```

---

<a name="приложения"></a>
## 12. Приложения

### Приложение A: Быстрый старт

**Минимальные шаги для запуска на одном компьютере:**

1. **Установите Python 3.8+** (если не установлен)
2. **Клонируйте проект:**
   ```bash
   git clone https://github.com/Leendeseqy/PKOvchinnikova_21IS_4semestr_Malinevskiy.git
   cd PKOvchinnikova_21IS_4semestr_Malinevskiy/messenger
   ```
3. **Создайте виртуальное окружение:**
   ```bash
   python -m venv venv
   # Windows:
   venv\Scripts\activate
   # Linux/Mac:
   source venv/bin/activate
   ```
4. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```
5. **Запустите сервер в первом окне терминала:**
   ```bash
   cd server
   python main.py
   ```
6. **Запустите клиент во втором окне терминала:**
   ```bash
   cd client
   python main.py
   ```
7. **Повторите шаг 6 для второго пользователя**

### Приложение B: Настройка статического IP для сервера

**Windows 10/11:**
1. Откройте `Параметры` → `Сеть и Интернет` → `Настройка параметров адаптера`
2. Правой кнопкой на активном подключении → `Свойства`
3. Выберите `IP версии 4 (TCP/IPv4)` → `Свойства`
4. Выберите `Использовать следующий IP-адрес`
5. Введите:
   - IP-адрес: `192.168.0.100`
   - Маска подсети: `255.255.255.0`
   - Основной шлюз: `192.168.0.1`
6. Нажмите `OK`

**Linux (Ubuntu):**
```bash
sudo nano /etc/netplan/01-netcfg.yaml

# Добавьте:
network:
  version: 2
  renderer: networkd
  ethernets:
    eth0:
      addresses:
        - 192.168.0.100/24
      gateway4: 192.168.0.1
      nameservers:
        addresses: [8.8.8.8, 8.8.4.4]

# Применить:
sudo netplan apply
```

### Приложение C: Открытие порта в брандмауэре

**Windows:**
1. Откройте `Панель управления` → `Система и безопасность` → `Брандмауэр Защитника Windows`
2. `Дополнительные параметры` → `Правила для входящих подключений`
3. `Создать правило` → `Для порта` → `TCP`, порт `8000`
4. `Разрешить подключение` → `Далее` → Название: `Local Messenger`
5. Нажмите `Готово`

**Linux (Ubuntu с ufw):**
```bash
sudo ufw allow 8000/tcp
sudo ufw reload
sudo ufw status
```

### Приложение D: Создание скриптов для быстрого запуска

**Скрипт для Windows (`start.bat`):**
```batch
@echo off
cd /d %~dp0
call venv\Scripts\activate
cd server
start python main.py
timeout /t 3
cd ..\client
start python main.py
```

**Скрипт для Linux/Mac (`start.sh`):**
```bash
#!/bin/bash
cd "$(dirname "$0")"
source venv/bin/activate
cd server
python main.py &
sleep 3
cd ../client
python main.py
```

---

## Заключение

Вы успешно установили и настроили **Локальный мессенджер**. Система готова к использованию в учебных целях.

**Важные напоминания:**
1. Сервер должен работать постоянно для работы клиентов
2. Все клиенты должны быть в одной сети с сервером
3. IP-адрес сервера должен быть правильно указан в конфигурации клиентов

**Для получения помощи:**
1. Проверьте логи сервера и клиента
2. Убедитесь в правильности сетевых настроек
3. Проверьте установку всех зависимостей
4. При необходимости обратитесь к документации в папке `../Документация/`

**Техническая поддержка:**
- Разработчик: Малиневский Егор Сергеевич
- Группа: 21ИС-24
- Назначение: Учебный проект

---

*Документ актуален для версии 1.0 проекта "Локальный мессенджер". Дата последнего обновления: 2026*
